"use strict"; // prisma.config.js

// Load environment variables (karena Prisma CLI tidak selalu meload dotenv secara otomatis)
require('dotenv').config();

// Export konfigurasi
module.exports = {
  datasources: {
    db: {
      adapter: 'sqlite',
      url: process.env.DATABASE_URL
    }
  }
}; /* v9-625dc24336dfc0c2 */
